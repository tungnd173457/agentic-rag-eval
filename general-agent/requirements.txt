# RAG pipeline (copy độc lập từ general-agent/backend) + eval harness.
# Chỉ gồm phần cần cho chunking + embedding + Weaviate + agentic retrieval.
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
structlog>=24.1
openai>=1.40                 # LLM chat + embedding (openai / ollama qua /v1)
weaviate-client>=4.21,<5     # vectordb (BYO vector, hybrid BM25+vector)
requests>=2.31               # reranker http client (khi USE_RERANKING=true)
pymongo>=4.6                 # chỉ là fallback get_full_text; KHÔNG bắt buộc chạy
